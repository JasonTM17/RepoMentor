import type { ReactElement } from "react";

const HomePage = (): ReactElement => (
  <main>
    <h1>RepoMentor</h1>
    <p>The web application scaffold is ready for the next phase.</p>
  </main>
);

export default HomePage;
